# Nunca commitar o .env real — apenas este .example
DATABASE_URL="postgresql://postgres:[SENHA]@db.kgtyfgiuyakixcuagdby.supabase.co:5432/postgres"
SUPABASE_URL="https://kgtyfgiuyakixcuagdby.supabase.co"
SUPABASE_ANON_KEY="[anon public key - pode ficar no cliente]"
# SUPABASE_SECRET_KEY nunca vai em .env versionado nem em repositório algum
CLAUDE_API_KEY="[chave da API Claude - só no backend, nunca no app mobile]"

# Web Push (avisos de vencimento) — gere seu próprio par com:
# node -e "console.log(require('web-push').generateVAPIDKeys())"
# A PUBLIC_KEY tem que ser IDÊNTICA à VITE_VAPID_PUBLIC_KEY do frontend.
VAPID_PUBLIC_KEY="[gere o seu par de chaves — ver comando acima]"
VAPID_PRIVATE_KEY="[gere o seu par de chaves — ver comando acima]"
